use geop_topology::topology::face::Face;

use crate::vertex_buffer::VertexBuffer;

pub fn rasterize_face_into_triangle_list(face: &Face, color: [f32; 3]) -> VertexBuffer {
    // Now we have to divide the face into triangles.
    todo!("Rasterize")
}