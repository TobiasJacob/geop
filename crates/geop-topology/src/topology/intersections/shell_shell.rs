use crate::topology::volume::{Volume, VolumeShellIntersection};

pub fn shell_shell_intersection(
    _volume_self: &Volume,
    _volume_other: &Volume,
) -> Vec<VolumeShellIntersection> {
    todo!("Volume::intersect")
}
