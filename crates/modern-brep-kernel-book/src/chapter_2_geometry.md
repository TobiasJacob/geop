
```rust
extern crate geop_geometry;

use geop_geometry::{
    curves::{circle::Circle, curve::Curve, line::Line},
    points::point::Point,
    surfaces::{plane::Plane, surface::Surface},
    transforms::Transform,
    EQ_THRESHOLD,
};

println!("Hello, world!");
        let v5 = Point::new(0.5, 0.5, 0.0);

```


